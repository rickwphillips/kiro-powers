{
  "name": "kiro-power-deploy-server",
  "version": "0.1.0",
  "description": "Embedded MCP server for the Deploy Kiro Power. Self-contained — not linked to mcp-skills.",
  "license": "MIT",
  "author": "Rick Phillips",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
